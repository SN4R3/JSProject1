// coming from ./data/products.js
console.log(products)

// Display Array of Objects coming in from /data/products.js
// inside of #products-container.

// Loop through the 'products' Array. For each iteration,
// nest another loop, this one will go through each Products's 'ingredients' key.

// For each iteration of the 2nd loop, append 
// the 'ingredient' to a String variable.

// After the 2nd nested loop, create a variable with Bootstrap's
// 'Card' HTML code (as a String)

// Where appropriote, insert our 'product' values
// inside the Card HTML.

// Once thats done, append the #product-container (via innerHTML)
// with our the variable containing the Card HTML

products.forEach((product) => {
    //looping through each 'product' Object

})